# Database
DATABASE_URL=postgresql://user:password@host:5432/dbname

# CORS (comma-separated). If unset, defaults to "*"
CORS_ALLOW_ORIGINS=http://localhost:3000

# Mock payment webhook secret used by /payments/webhooks/mock
PAYMENT_WEBHOOK_SECRET=change_me

# Optional: base site URL (used for redirect/callbacks if needed by future integrations)
SITE_URL=http://localhost:3000
